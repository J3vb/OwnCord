diff --git a/.superpowers/findings-ledger.json b/.superpowers/findings-ledger.json
index a05ac3af..7e57995d 100644
--- a/.superpowers/findings-ledger.json
+++ b/.superpowers/findings-ledger.json
@@ -10333,15 +10333,15 @@
       "file": "Server/ws/hub_broadcast.go",
       "line": 402,
       "severity": "medium",
-      "why": "docs/capacity.md publishes < 150 ms / < 300 ms for send -> sender acknowledgement and < 200 ms / < 400 ms for send -> recipient delivery, both met with room by the B6-9 steady profile (57/83 ms and 60/85 ms). The B6-10 operational profile is the same 100 connections and the same 2-vCPU cgroup with the storm, the voice churn and the upload scenario running alongside, and both budgets break: self_signed measured ack 228/348 ms and delivery 250/361 ms, tls off measured ack 329/472 ms and delivery 333/470 ms. These were the only two thresholds either run crossed. The server was not CPU-bound while it happened -- the cgroup averaged 0.41 of 2 CPUs (self_signed) and 0.31 (off), peaking at 1.23 -- so the cost is in the fan-out path, not the machine. The extra fan-out is the voice churn: 58,243 voice_state broadcasts against 12,044 chat sends, i.e. roughly five times as many broadcasts per second as the capacity profile carries.",
+      "why": "docs/capacity.md publishes < 150 ms / < 300 ms for send -> sender acknowledgement and < 200 ms / < 400 ms for send -> recipient delivery, both met with room by the B6-9 steady profile (57/83 ms and 60/85 ms). The B6-10 operational profile is the same 100 connections and the same 2-vCPU cgroup with the storm, the voice churn and the upload scenario running alongside, and both budgets break: self_signed measured ack 228/348 ms and delivery 250/361 ms, tls off measured ack 329/472 ms and delivery 333/470 ms. These were the only two thresholds either run crossed. The historical cgroup average was 0.41 of 2 CPUs (self_signed) and 0.31 (off), peaking at 1.23; averages alone do not exclude short bursts or database waits. Correction from the 2026-09-23 investigation: the 58,243 voice_state samples count recipient deliveries, not broadcast operations, so the original five-times-broadcast-rate inference was unsupported.",
       "repro": "gh workflow run load-baseline.yml --ref feat/b6-10-operational-measurements -f profile=operational, then read k6-summary.json from the constrained artifact: ws_broadcast_latency_ms p(95) 228 against its p(95)<150 threshold and ws_delivery_latency_ms p(95) 250 against p(95)<200.",
-      "evidence": "Run 35113946945 at e57335c7 (2026-09-16). Jobs 104854470744 (self_signed) and 104854471050 (tls off), constrained leg only. k6-summary.json: ws_broadcast_latency_ms thresholds p(95)<150 and p(99)<300 both ok:false on both legs; ws_delivery_latency_ms p(95)<200 ok:false on both, p(99)<400 ok:false on tls off. Published in docs/capacity.md, 'The operational profiles'. cpu.stat.log shows the cgroup never above 1.23 of 2 CPUs.",
+      "evidence": "Run 35113946945 at e57335c7 (2026-09-16). Jobs 104854470744 (self_signed) and 104854471050 (tls off), constrained leg only. k6-summary.json: ws_broadcast_latency_ms thresholds p(95)<150 and p(99)<300 both ok:false on both legs; ws_delivery_latency_ms p(95)<200 ok:false on both, p(99)<400 ok:false on tls off. Published in docs/capacity.md, 'The operational profiles'. cpu.stat.log shows the cgroup never above 1.23 of 2 CPUs. Investigation 2026-09-23 on dev 233b93f42ad941e00f43f2a62494707bdd30fe6e: voice audience resolution called Subject/HasActiveTimeout once per ordinary recipient despite CanViewChannel not using timeout state. A 25-participant leave/join burst against 100 members added 5,000 unnecessary DB reads. Before/after identical two-CPU-affinity Go benchmarks: audience median 1,986,457 -> 76,371 ns/op, 1,697 -> 97 allocs/op, 100 -> 0 timeout queries; HasActiveTimeout accounted for 86.34% of baseline audience CPU samples and none after. Ten unprofiled mixed bursts: median per-run ack p95 177.50 -> 113.40 ms, delivery p95 186.35 -> 122.60 ms, timeout reads 5,100 -> 100 (required chat checks retained). Raw samples, limitations, CPU/block/mutex summaries and reproduction commands: docs/performance/oc-0445/README.md. No qualifying k6 run: Docker/k6 unavailable; local queue-delivery timings exclude transport, TLS and storm handshakes and do not close this finding.",
       "status": "open",
       "found": "2026-09-16",
       "hunt": "operational-measurements-2026-09-16",
       "lens": "run-the-thing",
       "finder": "b6-10-operational-measurements",
-      "suggestedFix": "Profile the channel broadcast path under a voice_state-heavy fan-out before touching the budget. The budget is not to be loosened: it is met with 3x room by the steady profile on the same hardware, so the operational shape is doing something the steady shape does not."
+      "suggestedFix": "Use the visibility-only permission resolver to avoid irrelevant timeout reads while retaining live timeout checks for write/admission gates and existing role/override invalidation. Proposed fix and regression tests are accompanied by reproducible evidence in docs/performance/oc-0445/README.md. Keep OPEN until gh workflow run load-baseline.yml --repo J3vb/OwnCord --ref fix/oc-0445-operational-latency -f profile=operational -f users=250 -f connections=100 -f voice=25 supplies real acknowledgement/delivery p95/p99 for both constrained TLS legs. Do not relax budgets or rate limits."
     },
     {
       "id": "OC-0446",
